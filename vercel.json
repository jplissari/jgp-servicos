{
  "buildCommand": "cd client && pnpm install && pnpm run build",
  "outputDirectory": "client/dist/public",
  "installCommand": "cd client && pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://jgpservice.manus.space/api/:path*"
    }
  ]
}

